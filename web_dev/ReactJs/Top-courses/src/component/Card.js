/** @format */

import React from "react";

function Card({ courses }) {
	return (
		<div>
			<div>
        <img src={courses.image.url}></img>
        <div>
          <button>
            
          </button>
        </div>
			</div>
		</div>
	);
}

export default Card;
